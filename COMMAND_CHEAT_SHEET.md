# OpenClaw Command Cheat Sheet

- **Status**: `openclaw status` (Check if the gateway daemon is running and display current model/channel status).
- **Doctor**: `openclaw doctor` (Run a diagnostic check on system dependencies, permissions, and network connectivity).
- **Gateway Restart**: `openclaw gateway restart` (Restart the background daemon, useful if the agent stops responding or after config changes).